# RiQuest Host Releases

This public repository contains the update manifest and release assets for the private RiQuest Host desktop source repository.

The desktop app reads `latest.json` over HTTPS. Publish a Windows installer asset to a GitHub release first, then add its version, release URL, byte size, and SHA-256 checksum to the matching `platforms` entry.
